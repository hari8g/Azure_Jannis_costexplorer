# Contributing

- Run `./build.sh` before opening PRs.
- Keep scripts read-only with respect to cloud resources.
- Add/update docs when adding new commands.
- Prefer adding tests in `tests/run-tests.sh` or adjacent test scripts.
