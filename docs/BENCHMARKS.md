# Benchmarks

Measure wall-clock and memory of export parsing and governance checks.

## Example

```bash
time ./cur-audit.sh /path/to/export.csv --all
time ./phase4-governance.sh --report ./reports/<report>.json
```
